// Configuration SFTP — paramètres définis dans Config.toml ou via variables d'environnement
configurable string sftpHost = ?;            // Adresse du serveur SFTP
configurable string sftpUser = ?;            // Nom d'utilisateur SFTP
configurable string sftpPrivateKeyPath = ?;  // Chemin vers la clé privée SSH
